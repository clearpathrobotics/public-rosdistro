name: yamllint

on:
  push:
    branches: [master]
  pull_request:

jobs:
  yamllint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: pipx install yamllint
      - run: yamllint .
